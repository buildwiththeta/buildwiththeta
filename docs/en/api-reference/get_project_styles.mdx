---
title: Get project styles api
---

# Get project styles

To retrieve all the styles (colors, text styles) of a project, you can make a POST request to the 'get_project_styles' endpoint of the Build with Theta API. Here's a sample curl command to do that:

```bash
curl -X POST 'https://api.buildwiththeta.com/functions/v1/get_project_styles' \
     -H 'Authorization: Bearer eyJhbGciOi...' \
     -H 'Content-Type: application/json' \
     -d $'{
      "session_id": "23a9e8c7-cd8f-4557-...",
      "title": "Get styles",
      "description": "...",
      "properties": { ... },
      "device_info": {
        "os_name": "...",
        "os_version": "...",
        "locale": "...",
        "sdk_version": "...",
        "sdk_build_number": "..."
      }
    }'
```

Here are the parameters you need to provide in the JSON data:

- **session_id**: A universally unique identifier (uuid) assigned to a session.
- **title**: A string representing the title of the event. This title will be displayed inside the Insight panel under the Usage section.
- **description**: An optional string providing a description of the event.
- **properties**: An optional object containing additional properties related to the event.
- **device_info**: An object containing information about the client device.
  - **os_name**: A string representing the name of the operating system on the client device.
  - **os_version**: A string representing the OS version of the client device.
  - **locale**: An optional string representing the locale of the device.
  - **sdk_version**: A string indicating the Theta SDK version.
  - **sdk_build_number**: A string indicating the Theta SDK build number.

Please replace the placeholders (e.g., 'eyJhbGciOi...', '23a9e8c7-cd8f-4557-...', etc.) with the actual data you want to send in the request.
