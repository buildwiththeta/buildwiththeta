import 'package:flutter/material.dart';

/// Returns a Spacer widget
class OpenWSpacer extends Spacer {
  /// Returns a Spacer widget
  const OpenWSpacer({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => this;
}
