name: theta_analytics
description: Theta Analytics sdk for Flutter
version: 0.1.1

repository: https://github.com/buildwiththeta/buildwiththeta
homepage: https://buildwiththeta.com
issue_tracker: https://github.com/buildwiththeta/buildwiththeta/issues
documentation: https://docs.buildwiththeta.com

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  light_logger: ^0.0.3

  http: ^1.0.0
  uuid: ^3.0.6
  device_info_plus: ^9.0.2
  package_info_plus: ^4.0.2
  either_dart: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
