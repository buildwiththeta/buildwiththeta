name: theta
description: Build remote UIs.

version: 0.1.0-dev.2

repository: https://github.com/buildwiththeta/buildwiththeta
homepage: https://buildwiththeta.com
issue_tracker: https://github.com/buildwiththeta/buildwiththeta/issues
documentation: https://docs.page/buildwiththeta/buildwiththeta

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

scripts:
  gen: "flutter pub run build_runner build --delete-conflicting-outputs"

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  theta_analytics: ^0.1.0-dev.1
  theta_models: ^0.1.0-dev.1
  theta_rendering: ^0.1.0-dev.1
  theta_open_widgets: ^0.1.0-dev.1

  light_logger: ^0.0.3

  device_frame: ^1.0.0
  equatable: ^2.0.3
  either_dart: ^0.4.0
  http: ^0.13.5
  get_it: ^7.6.0
  provider: ^6.0.5
  collection: ^1.17.1
  hive: ^2.2.3
  meta: ^1.9.1

  reflectable: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  
  build_runner:
  source_gen:

flutter:
